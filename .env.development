# Entorno de desarrollo (`npm run dev`): modo conectado a través del proxy de Vite.
# Para cambiar algo solo en tu máquina, usa `.env.development.local` (ignorado por git).
VITE_API_URL=/api
VITE_BACKEND_PROXY=http://localhost:8080
