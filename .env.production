# Entorno de producción (`npm run build`).
# La URL real del backend desplegado (https://…/api) se define como variable de
# entorno en Vercel, que tiene prioridad sobre este archivo. Vacía, la build
# publicada funciona en modo demostración con datos simulados.
VITE_API_URL=
